# Internationalization

Translations live under `octoprint_uptime/translations`. Use Babel to extract
and compile translations (`pybabel` or `msgfmt`). The JS tooltip text uses
gettext calls where available.
