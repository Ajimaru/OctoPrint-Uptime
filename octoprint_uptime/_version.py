"""Centralized version constant for the OctoPrint-Uptime package.

This file exposes a single `VERSION` symbol which is used by
`__init__.py` and `plugin.py` as the single source of truth for the
package version. The packaging version is maintained in `pyproject.toml`
and kept in sync via `bump-my-version`.
"""

VERSION = "0.3.0"
